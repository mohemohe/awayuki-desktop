# 08. 実装状況レポート

- 更新日: 2026-07-14
- 対象: 本リファクタリング作業後の未コミット working tree
- 判定: `完了（ローカル検証済み）` は実装と対応する回帰テストがあり、統合後の macOS ローカル品質ゲートを完走した状態。`部分完了` は受入条件にコード、実測、外部 runner、追加分割の明確な残件がある状態。Windows/Linux、署名・公証、実 release runner の成功をローカル結果から推測しない
- 直近の統合済みローカル結果（2026-07-12）: Rust fmt/check/Clippy `-D warnings`、Rust 249 tests（1 ignored）、TypeScript、ESLint、Frontend 186 tests、production build、IPC/docs/architecture/portable/CSP/startup/release/dependency/bundle checks、420,000-status DB/system/reader-pool benchmark、10,000-status YQ allocation/page benchmark が成功。migration 032とICU4X indexer追加後の全体gate・42万件／実93万件再計測は未実施であり、この旧結果を現working treeの検証済み証跡として扱わない

## 変更不能なプロダクト契約

- 機能状態と資格情報の永続化先は `awayuki.db` だけである。access token、Bluesky session / app password、設定、キャッシュを Keychain、Credential Manager、Secret Service、registry、その他の OS store へ保存しない。任意の診断ログは機能状態ではない。
- アプリ停止後に `awayuki.db` を移動すれば、ログイン状態を含む機能状態を移行できる。WAL / SHM は実行時 sidecar であり、別の永続正本ではない。
- `PORTABLE`マーカーはWindows / Linuxだけの仕様である。macOSはマーカーを探索せず、release buildでは常にApplication Support配下を使用する。
- 自動 DB backup、別 DB recovery file、OS store からの migration / recovery path は作らない。本プロダクトは未リリースであり、存在しない旧方式の復旧互換性も持たせない。
- macOSはSparkle、WindowsはWinSparkleで公開releaseを確認して更新を通知する。OS preference /
  registry値はupdater metadataであり、資格情報・設定・Timeline等のAwayuki user dataは
  `awayuki.db`だけに保存する。
- DevToolsは利用者からconsole/network情報を含むbug reportを受け取るため、debug/releaseとOSを問わず意図的に有効化する。DevTools無効化をsecurity gateにしない。
- Home、Public、Notification は Unified Timeline である。Home は全 ActivityPub/Bluesky session、Public は全 ActivityPub session、Notification は全 session を統合する。active account は post/boost/favourite 等の操作主体だけであり、Timeline source を切り替えない。

この契約の実装・検査証跡は [README](../../../README.md)、[SQLite-only ADR](../../adr/0001-sqlite-only-portable-state.md)、[`credential_store.rs`](../../../src/auth/credential_store.rs)、[`storage_security.rs`](../../../src/state/storage_security.rs)、[`check-portable-state.mjs`](../../../scripts/check-portable-state.mjs) にある。

## 起動を 132646 ms 停止させた FTS migration 事故

`VERSION=9.9.9 ./scripts/build-app-bundle.sh` で作った app を既存 DB から起動した際、schema migration の DB phase が `132646 ms` 継続し、その間ウィンドウが利用可能にならなかった。原因は [`020_create_status_search_fts.sql`](../../../migrations/020_create_status_search_fts.sql) が FTS schema 作成と既存 `statuses` 全件 backfill を同じ blocking migration で実行したことだった。

対策は次の境界で固定した。

- [`desktop.rs`](../../../src/application/desktop.rs) は WebView を先に成立させ、migration、session restore、service startup を background worker へ移し、`app-startup-progress` を UI へ通知する。[`App.tsx`](../../../frontend/src/components/App.tsx) と [`App.test.tsx`](../../../frontend/src/components/App.test.tsx) は初期 snapshot より先に progress event を購読する。
- [`pool.rs`](../../../src/db/pool.rs) は legacy DB へ旧 migration の一括 data backfill を再実行せず、schema/checksum/transaction 契約を維持したまま data phase を分離する。
- [`023_resumable_status_search_backfill.sql`](../../../migrations/023_resumable_status_search_backfill.sql) は cursor、処理件数、完了状態を同じ `awayuki.db` に保持する。[`search_backfill.rs`](../../../src/services/search_backfill.rs) は小さい transaction に分割し、中断後に resume し、進捗 event を発行する。不完全な間の検索は正確性を落とさない fallback を使う。
- [`check-startup-boundaries.mjs`](../../../scripts/check-startup-boundaries.mjs) と [quality workflow](../../../.github/workflows/quality.yml) は blocking setup と旧一括 backfill の再混入を拒否する。

schema 25 の再試験では window ready が 920 ms まで改善した一方、起動後の無制限 retention と writer transaction 内の FTS virtual-table count が単一 writer pool connection を占有し、通常 write が 30,371 ms、retry 後 154,038--154,330 ms で timeout した。runtime から無制限 retention の自動呼出を削除し、FTS 完了判定を WAL reader の status counter / document / `docsize` 比較へ移した。旧実装の420,000-status fixtureでは24件更新がp95 2.397 msになったが、これはmigration 032後の再計測値ではない。その後、trigram/short n-gram自体の同期triggerとmergeが保存時間を再び増大させたため、migration 032で両triggerを停止し、activeなstatus更新・検索経路を同一DBのqueue、ICU4X index、`try_acquire`型live 8件／backfill 32件async indexerへ置換した。023/旧backfillと旧FTS tableはmigration履歴の互換schemaとして残るがproduction runtimeからは呼び出さない。安全なFTS5公開APIでは物理segmentの小分け廃棄ができないため、容量回収はoffline maintenanceの残件である。

同じ再試験で Custom SQL の固定 500 ms budget、YQ の固定 scan budget、DB persist 完了前の stream emit、Home/Public の legacy `account_acct` 誤解釈も確認した。SQL/YQ は専用 analytics reader と規模連動 budget へ移し、代表 YQ 候補抽出は実 DB read-only で 20.073 s 相当から 0.55 s、stream は WebView emit を永続化より先行、Home/Public/Notification は Unified 契約へ戻した。Bluesky Notification は初回を revision baseline のみにして過去通知を再通知せず、新規だけを設定間隔で emit する。

適用済み [`024_enforce_single_active_account.sql`](../../../migrations/024_enforce_single_active_account.sql) へ `IF NOT EXISTS` を後付けして checksum mismatch で起動不能にした回帰は、024を実DB履歴と同じ SHA-384 へ復元して解消した。履歴なしlegacy DBの同名indexは [`pool.rs`](../../../src/db/pool.rs) のschema-aware bootstrapだけで判定し、versioned migrationはappend-onlyに保つ。実DBの一時コピーでは024のchecksum検証後、[`026_decouple_global_columns_from_accounts.sql`](../../../migrations/026_decouple_global_columns_from_accounts.sql) のみを10 msで適用し、schema 26、Home/Public/Notification描画、Mastodon/Bluesky session復元を確認した。

## 正しさ・データ

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| SAFE-01 | 完了（ローカル検証済み） | read と mutation の retry policy を [`tauri.ts`](../../../frontend/src/api/tauri.ts) で分離し、応答喪失・二重実行を [`tauri.test.ts`](../../../frontend/src/api/tauri.test.ts) と [`mutationLifecycle.test.ts`](../../../frontend/src/domain/mutationLifecycle.test.ts) で固定。Mastodon/Paon投稿はoperation IDを`Idempotency-Key`へ、Bluesky投稿はSHA-256由来の決定的かつ有効なTID record keyへ変換し、同一operationを同じprovider identityへ収束させる。Bluesky create応答喪失・重複時は同じAT URIを再取得する。Misskey等の非対応providerは自動再送せずexactly-onceと扱わない。 |
| DATA-01 | 完了（ローカル検証済み） | immutableなversion/checksum/transaction と schema-aware legacy repair を [`pool.rs`](../../../src/db/pool.rs)、正規化・差分同期・resumable FTS・global column scopeを [`021`](../../../migrations/021_normalize_status_identity_and_viewer_state.sql)、[`022`](../../../migrations/022_incremental_startup_sync_and_read_models.sql)、[`023`](../../../migrations/023_resumable_status_search_backfill.sql)、[`026`](../../../migrations/026_decouple_global_columns_from_accounts.sql) で実装。自動 backup / recovery file は作らない。 |
| DATA-02 | 完了（ローカル検証済み） | login/logout/active account、column、status graph/timeline entry、cache clearをtransaction / serialized lifecycleへ集約。各途中失敗をSQLite triggerで注入し、[`settings.rs`](../../../src/db/queries/settings.rs)と[`timeline_service.rs`](../../../src/services/timeline_service.rs)で既存account・Unified/global column・status/account/tag/timeline entryがbefore状態へrollbackされることを検証。存在しないsessionはDB変更前に拒否され、IPCで`authentication_expired`へ分類される。UIは[`mutationLifecycle.test.ts`](../../../frontend/src/domain/mutationLifecycle.test.ts)で失敗・応答喪失を確定扱いしない。 |
| CONF-01 | 完了（ローカル検証済み） | [`ipc/contract.rs`](../../../src/ipc/contract.rs)を設定key・schema version・Rust型名・`SettingsSnapshot` fieldの正本にし、[`generate-ipc-contract.rs`](../../../src/bin/generate-ipc-contract.rs)からTypeScript descriptor/key mappingを生成。未知key・型違い・正規化不能値を保存前に拒否し、破損JSONは同じ`awayuki.db`内で診断可能に退避する。v0→v1 migrationは既存JSONを変更せず`_settings_schema_version`だけをSQLiteへ記録し、新しいversionのdowngradeを拒否するtestを[`settings.rs`](../../../src/db/queries/settings.rs)に追加。 |
| ERR-01 | 完了（ローカル検証済み） | stable code、safe message、code + command policy retry、request/operation IDを[`ipc/error.rs`](../../../src/ipc/error.rs)、[`observability.rs`](../../../src/observability.rs)、[`ipcErrors.ts`](../../../frontend/src/api/ipcErrors.ts)に実装。Frontend全invokeがoperation ID headerを送り、DTOなし/top-level/sync commandを含むthin IPC handlerは共通observerで同じIDをstart/complete/errorとsafe envelopeへ維持する。malformed IDとraw causeはUI/logへ未加工で出さない。 |
| ASYNC-01 | 完了（ローカル検証済み） | startup/stream generationに加え、Timeline/profile/autocompleteをresource-key generation・有界scheduler・7状態へ統一。caller-owned operation IDと`cancel_timeline_query`でscheduler `AbortSignal`をRust cancellation registryへ接続し、Custom SQL/YQはSQLite progress handlerまでcancelする。DBへ書くprovider refresh/load-moreはHTTP fetch中とpage境界でcancelし、fetch成功後のin-flight transactionだけをdropせず各commit通知を保証する。media upload/downloadとloginも専用cancel経路を持つ。Active account切替はUnified Timelineを維持してviewer stateだけを照合する。 |

## セキュリティ・信頼境界

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| CRED-01 | 部分完了 | Unix mode / umask と Windows current-user protected DACL、DB/WAL/SHM 一括補正、FAT/exFAT 非対応時 warning を [`storage_security.rs`](../../../src/state/storage_security.rs) に実装。macOS/Linux test は通過、Windows `cfg` integration test は追加済みだが Windows 実 runner の完走が残る。 |
| SEC-01 | 完了（ローカル検証済み） | HTML tag/attribute/class/scheme allowlist と custom emoji URL 制限を [`CustomEmoji.tsx`](../../../frontend/src/components/common/CustomEmoji.tsx) に集約。SVG、壊れ HTML、mention、hashtag、改行、危険 scheme を [`CustomEmoji.test.tsx`](../../../frontend/src/components/common/CustomEmoji.test.tsx) で検証。 |
| SEC-02 | 完了（ローカル検証済み） | 資格情報の SQLite-only persistence、rotation/logout serialization、DB 移動 test を [`credential_store.rs`](../../../src/auth/credential_store.rs) に実装し、[`check-portable-state.mjs`](../../../scripts/check-portable-state.mjs) と [ADR](../../adr/0001-sqlite-only-portable-state.md) で OS store / 別 backup を禁止。 |
| SEC-03 | 完了（ローカル検証済み） | listener所有、state定数時間比較、timeout、single-use callbackを[`callback_server.rs`](../../../src/auth/callback_server.rs)、S256 PKCEと非対応endpoint互換testを[`oauth.rs`](../../../src/mastodon/oauth.rs)に実装。LoginViewのCancel/closeをcaller-owned operation IDと`cancel_login_flow`へ接続し、future dropでlistener解放・transaction rollbackすることを[`LoginView.test.tsx`](../../../frontend/src/components/auth/LoginView.test.tsx)と[`operation_cancellation.rs`](../../../src/state/operation_cancellation.rs)で固定。code/verifier/auth URLはlogへ出さない。 |
| SEC-04 | 完了（ローカル検証済み） | Browser uploadを256 KiB以下のraw `Uint8Array` IPC、private temp file、provider multipart streamへ接続し、JSON `number[]`と全量copyを撤去。drop path capability、magic/MIME/extension/size検査、upload progress/cancel/blob revokeを[`mediaUpload.ts`](../../../frontend/src/api/mediaUpload.ts)と[`media_upload.rs`](../../../src/state/media_upload.rs)に実装。downloadは[`operation_cancellation.rs`](../../../src/state/operation_cancellation.rs)と[`MediaPreviewOverlay.tsx`](../../../frontend/src/components/media/MediaPreviewOverlay.tsx)でoperation ID単位のprogress/cancel、timeout/上限、redirect再検証、incremental private part、atomic publish、失敗時cleanupを実装。 |
| SEC-05 | 完了（ローカル検証済み） | origin 固定、popup/download deny、style/lifecycle owner を [`sidecar_policy.rs`](../../../src/application/sidecar_policy.rs)、[`sidecar.ts`](../../../frontend/src/domain/sidecar.ts)、[`WorkspaceView.tsx`](../../../frontend/src/components/workspace/WorkspaceView.tsx)、[`sidecar.test.ts`](../../../frontend/src/domain/sidecar.test.ts) に実装。 |
| SEC-06 | 部分完了 | macOS Sparkle / Windows WinSparkle dependency、起動時background check、Framework / x64 DLL同梱処理、両OS appcastを復元。macOSは実bundleのFramework、load command、Info.plist、署名をローカル検証済み。Windowsは依存解決とpackage経路を復元したが、Windows runnerでのbuild完走確認が残る。 |
| SEC-07 | 完了（ローカル検証済み） | locked release build、download digest、source/artifact manifest、action SHA pin を [`build-appimage.sh`](../../../scripts/build-appimage.sh)、[`artifact-manifest.mjs`](../../../scripts/artifact-manifest.mjs)、[shared artifact workflow](../../../.github/workflows/build-artifacts.yml)、[`check-release-boundaries.mjs`](../../../scripts/check-release-boundaries.mjs) で検査。 |
| SEC-08 | 完了（ローカル実装検証済み） | reviewed entitlement と audit を [`Entitlements.plist`](../../../resources/Entitlements.plist)、[`audit-macos-entitlements.sh`](../../../scripts/audit-macos-entitlements.sh)、[entitlement 文書](../../security/macos-entitlements.md) に固定。release workflow は署名済み app から `macos-entitlements.plist` を抽出し、2項目だけのallowlist一致を検査して配布artifactへ保存する。Apple notarization / staplerを含む実release run自体は外部runnerの最終ゲートである。 |
| SEC-10 | 部分完了 | deny-default CSP と capability 境界を [`tauri.conf.json`](../../../tauri.conf.json)、[`default.json`](../../../capabilities/default.json)、[`sidecar_policy.rs`](../../../src/application/sidecar_policy.rs) に実装。[CSP policy](../../security/csp-policy.md)へsourceごとの必要性・threat model・削除条件を記録し、[`check-csp-policy.mjs`](../../../scripts/check-csp-policy.mjs)と[`audit-csp-sources.mjs`](../../../scripts/audit-csp-sources.mjs)でdeny directive、source consumer、script/connect/media allowlistをCI検査する。package runtime attestationは実artifactのdeny-default、外部connect不在、remote media source維持を確認し、loopback fixtureは実WebViewでMastodon/Misskey/Paon/Bluesky別remote image、custom emoji、MP4 decodeとnative Sidecar create→preview hide→restore→closeを実行して3 OS reportへ残す。macOS ad-hoc release bundleでは全項目、schema 28起動、`securitypolicyviolation=0`を確認済み。Windows/Linux runner完走が残る。 |

## バックエンド・データ設計

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| ARCH-01 | 部分完了 | 入口を[`tauri_commands.rs`](../../../src/tauri_commands.rs)のshimと`ipc/*` handlerへ分離し、adapter/port、application、queryを[`api/ports.rs`](../../../src/api/ports.rs)、[`application`](../../../src/application)、[`db/queries`](../../../src/db/queries)へ配置。account/compose（各DTOを含む）/maintenance/media/notification/auth/preferences/runtime/settings/status/timeline/timeline_view/translation familyを[`account.rs`](../../../src/application/account.rs)、[`compose.rs`](../../../src/application/compose.rs)、[`maintenance.rs`](../../../src/application/maintenance.rs)、[`media.rs`](../../../src/application/media.rs)、[`notification.rs`](../../../src/application/notification.rs)、[`auth.rs`](../../../src/application/auth.rs)、[`preferences.rs`](../../../src/application/preferences.rs)、[`runtime.rs`](../../../src/application/runtime.rs)、[`settings.rs`](../../../src/application/settings.rs)、[`status.rs`](../../../src/application/status.rs)、[`timeline.rs`](../../../src/application/timeline.rs)、[`timeline_view.rs`](../../../src/application/timeline_view.rs)、[`translation.rs`](../../../src/application/translation.rs)へ分離し、IPC decode→application orchestration→state/repositoryの依存方向を固定した。認証provider API完了→portable SQLite transactionの順序とprovider失敗時にtransactionを開始しないことをtestした。設定runtime effectもportable SQLite commit成功後だけ実行し、失敗時はskipする。起動はfrontend-ready handshake後にbackground workerを開始し、migration/session restoreをruntime application境界へ分離した。session restoreはportable SQLiteから全accountを読み、Active accountをactorとして設定しても全signed-in sessionからUnified streamを再開する。maintenanceはDTOを所有し、support bundleをfileへ保存せずメモリ上で返す。Active account切替は操作元だけを変更してstreamを維持し、logoutだけがUnified source集合を再構成する。status mutationはactorとcanonical identityを分離してviewer stateをactor単位で保存する。compose uploadはbegin時のactorへ固定し、finish時のActive accountを参照しない。Timeline load/load-more/refreshとlocal種別選択をapplicationへ移し、Home/Public/Notificationは`TimelineType::is_unified`でlegacy account bindingに関係なくUnifiedと分類する。Notification read APIはaccount selectorなしで全source contextを読む。account-bound timeline entryとbookmark/favourite SQLはIPC非依存の[`timeline_views.rs`](../../../src/db/queries/timeline_views.rs)へ、検索SQLは[`search.rs`](../../../src/db/queries/search.rs)へ、YQ scan/evaluatorは[`yq_timeline.rs`](../../../src/services/yq_timeline.rs)へ移した。Timeline DTOとAPI/SQLite Status/Notification純粋変換をtimeline_viewへ、SQLite status/account一括取得・reblog/quote hydration・source/viewer state適用を[`timeline_hydration.rs`](../../../src/application/timeline_hydration.rs)へ分離した。profile/pinned timeline/AIR/threadは明示sourceを使い、同一domain複数sessionで未指定ならcache-onlyへ落とす。設定schema・破損値退避・snapshotはportable SQLiteだけへ依存する。`desktop.rs`は5,429行となり、account/compose DTOの分離を完了した。platform runtime・stream supervisor等の追加分割は継続する。 |
| ARCH-02 | 完了（ローカル検証済み） | Rust command/capability registryに加え、[`ipc/dto.rs`](../../../src/ipc/dto.rs)のmacroからserde field名・optionality・TypeScript DTO・command args/result mapを[`generate-ipc-contract.rs`](../../../src/bin/generate-ipc-contract.rs)で生成。全JSON commandとraw media commandをtyped registryへ登録し、Frontendのgeneric mutation/read/operation-ID APIを削除した。identity/contentとacting accountを別fieldで固定し、raw chunkは生成済みresult型と`Uint8Array` IPCを維持する。[`contract.test.ts`](../../../frontend/src/api/contract.test.ts)とRust generator testがcompile-time signature・全command coverageを検証する。 |
| ROUTE-01 | 完了（ローカル検証済み） | server-aware status identity、acting account、viewer state と Unified Home/Public/Notification を [`identity.rs`](../../../src/domain/identity.rs)、[`desktop.rs`](../../../src/application/desktop.rs)、[`appStore.timeline.test.ts`](../../../frontend/src/store/appStore.timeline.test.ts) で固定。profile/thread/AIRはstatusの`sourceAcct`をrequestへ保持し、同一domain複数sessionでも明示sourceを選ぶ。Active account切替はTimeline/paneを維持したまま、`status_viewer_states`で`protocol + canonical_uri`に対応する新actorのfavourite/boost/bookmark状態だけを原子的に再評価する。 |
| ARCH-03 | 完了（ローカル検証済み） | protocol-neutral DTO、adapter error、capability snapshot、port を [`protocol.rs`](../../../src/domain/protocol.rs)、[`adapter_error.rs`](../../../src/domain/adapter_error.rs)、[`capability.rs`](../../../src/domain/capability.rs)、[`api/ports.rs`](../../../src/api/ports.rs) に導入。 |
| DATA-03 | 完了（ローカル検証済み） | canonical composite identity、viewer state、status-tag relation、orphan cleanup / FK を [`021`](../../../migrations/021_normalize_status_identity_and_viewer_state.sql)、[`models.rs`](../../../src/db/models.rs)、[`statuses.rs`](../../../src/db/queries/statuses.rs)、[`tags.rs`](../../../src/db/queries/tags.rs) へ反映。 |
| SQL-01 | 完了（ローカル検証済み） | SQLite authorizer、VM/wall-clock budget、result/payload cap、plan inspectionに加え、frontend `AbortSignal`→operation ID→`cancel_timeline_query`→SQLite progress handlerの実cancelを[`custom_timeline.rs`](../../../src/db/queries/custom_timeline.rs)、[`appStore.ts`](../../../frontend/src/store/appStore.ts)、生成IPC contractに実装。validationはraw SQLを返さず、scannerが特定したquery行・桁だけをallowlist済みsafe detailへ載せる。 |
| AUTH-01 | 完了（ローカル検証済み） | Bluesky refresh single-flight、成功/失敗共有、auth generation、rotation persistence と logout/re-login の stale write rejection を [`bluesky/client.rs`](../../../src/bluesky/client.rs) と [`credential_store.rs`](../../../src/auth/credential_store.rs) の concurrency test で検証。 |
| DEAD-01 | 完了（ローカル検証済み） | 旧 GPUI state/assets と repository-wide `dead_code` allow を削除し、[`Cargo.toml`](../../../Cargo.toml) と [architecture](../../architecture.md) を現行化。`cargo clippy --all-targets --locked -- -D warnings` は統合後ゼロ警告。 |

## フロントエンド設計

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| FE-01 | 完了（ローカル検証済み） | window-first background initialization、`listeners/snapshot/timelines/complete` stage、段階別safe error/retry、critical listenerの全登録前handshake禁止、root Error Boundaryとdiagnostics copyを[`startup_gate.rs`](../../../src/application/startup_gate.rs)、[`desktop.rs`](../../../src/application/desktop.rs)、[`App.tsx`](../../../frontend/src/components/App.tsx)、[`AppErrorBoundary.tsx`](../../../frontend/src/components/common/AppErrorBoundary.tsx)と各testに実装。 |
| FE-02 | 完了（ローカル検証済み） | normalized status entity と mutation reconciliation を [`timelineEntities.ts`](../../../frontend/src/domain/timelineEntities.ts)、[`timelineEntities.test.ts`](../../../frontend/src/domain/timelineEntities.test.ts)、[`appStore.timeline.test.ts`](../../../frontend/src/store/appStore.timeline.test.ts) で固定。 |
| FE-03 | 完了（ローカル検証済み） | settings draft、serialized mutation、resource-local error を [`settingsMutations.ts`](../../../frontend/src/domain/settingsMutations.ts)、[`settingsDraft.ts`](../../../frontend/src/store/slices/settingsDraft.ts) と各 test に実装。 |
| FE-04 | 完了（ローカル検証済み） | sidecar operation generation、serialized/coalesced layout、close cleanup、navigation/reload/style ownershipを[`sidecar.ts`](../../../frontend/src/domain/sidecar.ts)と[`WorkspaceView.tsx`](../../../frontend/src/components/workspace/WorkspaceView.tsx)に集約。style retryはbounded backoffとし成功/close/unmountでtimerを停止。 |
| FE-05 | 完了（ローカル検証済み） | instance login と Bluesky app-password login の submit intent / validation を [`LoginView.tsx`](../../../frontend/src/components/auth/LoginView.tsx) と [`LoginView.test.tsx`](../../../frontend/src/components/auth/LoginView.test.tsx) で分離。 |
| UI-01 | 完了（ローカル検証済み） | operation ID付きmutation phase、double-click coalescing、confirmation、共通toast、scope invalidation/uncertain resultを[`mutationLifecycle.ts`](../../../frontend/src/domain/mutationLifecycle.ts)へ、dialog ID付きFIFOとunmount cleanupを[`confirmationQueue.ts`](../../../frontend/src/domain/confirmationQueue.ts)、[`ConfirmationDialog.tsx`](../../../frontend/src/components/common/ConfirmationDialog.tsx)へ集約。並行要求、二重click、unmount、response lossをtestで検証。 |
| FE-06 | 完了（ローカル検証済み） | compose/panes/resources/session/settings等のpure reducerを[`store/slices`](../../../frontend/src/store/slices)へ、session・pane・compose target/submit・settings/column・status mutation・timeline query/stream・lifecycle orchestrationを[`store/actions`](../../../frontend/src/store/actions)へ分離。status mutationはActive accountを操作元に限定し、queryはUnified列のload/load-moreで`accountAcct`を省略、streamはsource account別の順序/gapを追跡しながらUnified Home/Public/Notificationの全column反映を維持する。actionの`AppStore`参照はtype-onlyで、dynamic pane descriptor集約とunrelated resource error保持をtestで固定し、public contractを維持。`appStore.ts`は2,596行から841行へ縮小。 |
| FE-07 | 完了（ローカル検証済み） | Compose/Settings/Timelineをcontroller/view/featureへ分離し、[`compose`](../../../frontend/src/features/compose)、[`settings`](../../../frontend/src/features/settings)、[`timeline`](../../../frontend/src/features/timeline)とlayout fixture testを追加。Compose media lifecycleは[`useComposeMediaQueue.ts`](../../../frontend/src/features/compose/useComposeMediaQueue.ts)へ集約し、Active account切替時の旧upload abort/late result破棄をhook testで固定。 |
| FE-08 | 完了（ローカル検証済み） | timeline typeのlabel/load/pagination/stream/filter/parameter/capabilityを[`timelineDescriptors.ts`](../../../frontend/src/domain/timelineDescriptors.ts)に集約し、全signed-in sessionのcapability unionで作成候補を決定してActive accountによるUnified Timeline narrowingを禁止。[`timelineDescriptors.test.ts`](../../../frontend/src/domain/timelineDescriptors.test.ts)でmetadata exhaustiveness、複数protocol union、未知値fallbackを検証。 |
| FE-09 | 完了（ローカル検証済み） | semantic i18n key、日英catalog、locale候補/runtime切替、app-locale Intl formatterを[`i18n.ts`](../../../frontend/src/i18n.ts)とtestで固定。notification kindは[`notification.ts`](../../../frontend/src/domain/notification.ts)でprovider spellingを正規化し、label非依存のicon/timestamp/filter分岐に統一。IPC errorはtyped safe messageだけを表示する。 |
| FE-10 | 完了（ローカル検証済み） | strict mock command exhaustiveness と production marker scan を [`mock.ts`](../../../frontend/src/api/mock.ts)、[`contract.test.ts`](../../../frontend/src/api/contract.test.ts)、[`check-bundle-budget.mjs`](../../../scripts/check-bundle-budget.mjs) に実装。 |
| FE-11 | 完了（ローカル検証済み） | Dialog/Listbox/Menu/Tabs、focus utility、LiveRegionを[`components/primitives`](../../../frontend/src/components/primitives)へ共通化。axeとkeyboard/focus regressionに加え、Compose mediaのkeyboard reorder/announcementをcomponent testで固定し、reduced-motionとtext selection policyをCSSで明示。 |

## パフォーマンス

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| PERF-01 | 部分完了 | high-water/cursor、warm skip、full reconciliation resume、checkpoint を [`startup_sync.rs`](../../../src/services/startup_sync.rs) と [`022`](../../../migrations/022_incremental_startup_sync_and_read_models.sql) に実装。420k同一DB warm fixtureは旧/現行でAPI 18/0、writes 18/0、ready 4.324/3.175 ms、DB+WAL増加0/0 bytesを記録した。retention sweepは単一writerを長時間占有し得るためproduction自動実行から撤回済みで、保護条件と1 sweep上限のtest fixtureだけを維持する。writerを飢餓させないproduction prune設計が残る。 |
| PERF-02 | 完了（ローカル検証済み） | Home/Public/Notification、startup sync、profile pinned、AIR、thread、post/action/editはstatus本体を先に保存・返却し、quote network lookupをsource account内canonical identity dedupe、最大128 job、全体同時4件・source server単位同時1件、5秒timeout、3 attempt、jitter、5分negative cacheのbackground workerへ限定した。DTO/eventは`pending / resolved / unavailable`を明示し、同期hydrationのapplication層再混入を`startup:check`で拒否する。typed `quoteConsumerId`とgeneration付きowner参照によりcolumn/profile pane closeは不要jobだけを停止し、共有account ownerは維持する。logoutは当該sourceのconsumer jobも停止するがActive account切替やstream再構成は停止しない。initial latency・dedupe・共有owner・pane/account cancel・unavailable UI・server budgetをtestした。 |
| PERF-03 | 完了（ローカル検証済み） | raw/persistence/quote/WebView emit/notification side-effectをbounded化し、identity coalescing、generation clock、emit-before-persistence、overflow lag、回復後Resync→Unified全column reloadをbackend/frontend testで固定。不要Public購読を開かず、Mastodon/Misskey reconnectはtimeout/backoff/jitter/server spacingを共有する。seed v3 420k・100 events/s burstはqueue 100、drop/resync 0、3,958 events/s、RSS delta 12,779,520 bytesで64MiB予算内。 |
| PERF-04 | 部分完了 | Bluesky status/notificationをSHA-256 revision diffし、window脱落は30分・最大4件のreconciliation queueで削除確認する。migration 027のportable SQLite checkpointへaccount/stream別baselineとqueueを保存し、同一内容はwrite 0、logoutはFK cascade、再起動notification replay抑止、未保存statusのcheckpoint除外をtestした。固定1時間fixtureは`0bb67a2`比でAPI calls 120→120、DB writes/events各4,800→0。notification hydrationは512件・取得時刻基準10分のpositive cacheと512件・30秒のnegative cacheをprocess memoryだけに持ち、mutation後は最新viewer stateで置換する。反復poll call-count fixtureとpage API callsの桁違い削減は未達。 |
| PERF-05 | 完了（ローカル検証済み） | page/eventのtimeline/status/account/tag writeを件数・時間上限付きtransaction batchへ統合し、batch内identity/tagを重複排除。同じ1,000 statusを旧相当1件呼出しとpage batchで別WAL DBへ保存し、statements/commits/wall/WALを記録してtransaction・statement削減と無欠落を検証。 |
| PERF-06 | 部分完了 | migration 032でtrigram/short n-gramの同期status triggerを停止し、migration 034でaccount検索も専用portable ICU FTS/content/queue/backfillへ移した。migration 035は既存postingsを消去せずcursorだけをO(1) resetし、punctuation/emoji tokenを非同期更新する。status/account live 8件、backfill 32件単位の低優先度async indexerはwriterを`try_acquire`だけで取得し、ICU4X 2.1.1のNFKC・Unicode case folding・dictionary segmentationをwriter取得前に実行する。128-bit generation、source field照合、cursor CASでstale indexを防ぎ、両FTSのautomatic mergeをportableな個別有限creditのbounded mergeへ分離した。word-like tokenと非空白punctuation/emoji segmentを単一FTS tokenへ符号化し、検索・更新経路から文字n-gramを除外した。最大8 termの候補計数と実candidate branchをstatus/account各10,000件でmaterializeし、pendingと移行gapのICU scalar評価は各256件、保存済みtokenによるrecency補完は最新10,000 statusへ限定する。bounded sourceをrecency順にしてからcandidate上限を適用し、前景で全status/accountを再分節・展開しない。keyset、query cancel・10秒budgetを維持する。42万件fixture・実93万件DBのp95再計測と安全なoffline容量回収が残る。 |
| PERF-07 | 部分完了 | query単位compile、EvaluationCache、SQL prefilter、keyset、規模連動budget、cancellation、metrics UIを実装した。`created_at`はlocal change revisionではないためdelta cursorに使わず、64件／40ms上限の各SQLite transaction commit直後に送る`timeline-cache-committed`を受けてcoalesce後に全結果を置換する。migration 030は安定走査順序用indexである。安全なdeltaにはlocal change revisionと非match reconciliationが残る。 |
| PERF-08 | 完了（ローカル検証済み） | notification context 3 statement、recursive thread 1 statement、limit-first aggregate、write-time counter を [`read_models.rs`](../../../src/db/queries/read_models.rs)、[`022`](../../../migrations/022_incremental_startup_sync_and_read_models.sql) と benchmark に実装。 |
| PERF-09 | 部分完了 | canonical entity、明示的load-more pageを切り捨てない無制限pagination、near-top復帰時だけの`maxStatuses` trim、rAF/40ms micro-batch、identity coalescing、far-anchor/unread維持を実装した。trim後のlocal offsetは保持件数へ戻り、破棄したpageを再取得する。実93万件DBでCustom 78回/YQ 26回・169.354 query-seconds/3分・main process約200%を確認したため、commit前のlive eventによるquery実行を廃止した。各SQLite transaction commit直後の通知を正本としてCustom/YQをdirty化し、backend single-flightとfrontend waveの二段でcoalesceする。bounded emit queue満杯時も待機taskは1つだけで、IPC成功後の保守的通知も同じwaveへ吸収する。各paneで選択中かつnear-topのcolumnだけを2秒coalesce、30秒cooldown、version保持、analytics lane同時1件でrefreshする。失敗versionは周期再試行せず、hidden/scrolled columnはdirty/unreadだけを保持する。Unified Home/Public/Notificationは即時stream反映を維持する。部分commit、notificationの段階commit、response cancel、満杯queueで240通知を1件へまとめる経路とtransaction数一致をRust testで固定した。同DB packaged再計測が残る。 |
| PERF-10 | 完了（ローカル検証済み） | translation/blurhash/media retryをitem/byte・TTL付きweighted LRUへ統一し、login/logout時にaccount-scoped clearする。media retryはURL single-flight/negative cache/jitterとAbortSignal consumerを共有し、最後のoffscreen consumerが消えた未開始timerを中止する。翻訳は[`translationScheduler.ts`](../../../frontend/src/features/timeline/translationScheduler.ts)でvisible/manual priority、同時3件、content/engine generation単位single-flight、consumer cancelへ集約し、viewport近傍だけauto translateする。8時間synthetic scrollでcache上限とtimer 0を検証。 |
| PERF-11 | 完了（ローカル検証済み） | feature chunk、raw/gzip/Brotli budget、mock marker scanに加え、startup-only packaged WKWebViewを監査前`0bb67a2`と現在版へ同一instrumentationで適用した。package WebView smoke追加後のinitial JS raw/gzipは550.82/168.63→469.24/148.61kBで、smoke本体は2.81/1.35kBのlazy chunk、通常initial増分は0.21/0.11kB。可視化後interactiveは376→372ms。一方module evaluate 314→339ms、初回commit 321→346ms、main process RSS 112,181,248→121,044,992 bytesの退行もartifactと[baseline](../../performance-baseline.md)へ記録した。SQLite/OS store/side fileへmetricsを保存しない。 |
| PERF-12 | 完了（ローカル検証済み） | shared reqwest policy、timeout、response size capを[`api/http.rs`](../../../src/api/http.rs)へ、read-only Timeout/Transport/429 retryを[`api/retry.rs`](../../../src/api/retry.rs)へ統一。最大3 attempt、Retry-After、決定的jitter、同一server spacing、256-server上限を持ちmutationは再送しない。timeline/profile/autocomplete、login、media upload/downloadに加え、post/status action/vote/edit/delete/followをoperation tokenへ接続した。account scope変更は同じoperation IDのprovider futureをcancelしてUIをuncertainに保ち、Tauri終了はquery/login/media/mutation/quote tokenとstream taskを停止する。pending provider futureのdropとno-retryをtestした。 |
| PERF-13 | 完了（ローカル検証済み） | timeline/profile/autocomplete lane別priority/concurrency、resource generation cancelを実装。重いCustom/YQだけを直列analytics laneへ分離し、reader pool 500と通常read並列性は維持する。autocompleteは250ms debounce・2文字・8件上限、profile4資源はbounded並列開始する。プロフィールchromeを共通Virtuosoのscroll-away Headerへ統合し、投稿0件を含めstatusと同じ1つのscroll ownerで動かす。scroll-to-topもprofile最上部を対象とし、DOM containmentを回帰testで固定した。near-topは値変更時のみdispatchする。Frontend 186件、Rust 249件、Clippy、typecheck、lint、production buildを通過した。既存macOS packaged値はstream commit/frame p95 10/33ms、scroll 66ms、profile 8/4msで、分析query storm修正後の実DB再計測はPERF-09に残す。 |
| PERF-14 | 完了（ローカル検証済み） | 2,048-record非同期queue、256KiB record、5MiB/24時間/3世代rotation、overflow metrics、release info/opt-in file log、verbose hot path 1/16 samplingを[`logging.rs`](../../../src/state/logging.rs)へ実装。frontendはproduction log/debug除外と100ms/50件/16KiB batchを行う。secret/OAuth/Bearer/Authorization/content/notification/local pathをbackend/frontendでredactし、runtime diagnosticのignore/package exclusionをgate化した。16,384-event release fixtureはoff/on producer p95 0.000042/0.001708 ms、on throughput 1,242,801 events/s、drop 0で、CI artifactにも保存する。 |
| PERF-15 | 部分完了 | 2/4接続partitionが実クライアントでtimeoutしたため、通常/analyticsを500接続の共有lazy WAL reader poolへ統合した。`u32::MAX`で約100GB VMを予約する誤りも撤回済み。window stateの1 worker/debounce/close flushとSidecar layout/style timer集約は維持する。500接続設定での実app acquire wait/RSS継続計測が残る。 |

## 品質・運用・配布

| Task | 状態 | 実装・テスト・CI 証跡 |
| --- | --- | --- |
| QUAL-01 | 完了（ローカル検証済み） | fmt、Clippy `-D warnings`、Rust/Frontend test、typecheck、lint、build、contract/docs/portable/startup/release check を [quality workflow](../../../.github/workflows/quality.yml) と [`package.json`](../../../package.json) に必須化。 |
| QUAL-02 | 部分完了 | migration、OAuth、sanitizer、IPC retry、routing、async generation、accessibility、performance fixture を Rust unit test と [`frontend/src`](../../../frontend/src) の Vitest 群へ追加。 |
| OPS-01 | 完了（ローカル検証済み） | UI→IPC→API→DB operation ID、phase duration、startup/queue/sync/query/retry/render metrics、stream gap/resync UI、redacted support snapshotを[`observability.rs`](../../../src/observability.rs)、[`api/observability.ts`](../../../frontend/src/api/observability.ts)、[`diagnostics.ts`](../../../frontend/src/api/diagnostics.ts)に実装。support payloadはメモリ上だけで、SQLite/OS store/side fileへ保存しない。 |
| REL-01 | 完了（ローカル検証済み） | reusable 3 OS build、deterministic source archive、manifest digest/size/version、appcast invariant を [artifact workflow](../../../.github/workflows/build-artifacts.yml)、[`artifact-manifest.mjs`](../../../scripts/artifact-manifest.mjs)、[appcast workflow](../../../.github/workflows/update-appcast.yml) に実装。 |
| REL-02 | 部分完了 | macOS unsigned build→protected signing/notarization→secretless smoke を [artifact workflow](../../../.github/workflows/build-artifacts.yml) で job 分離し、[`check-release-boundaries.mjs`](../../../scripts/check-release-boundaries.mjs) で ref/権限境界を検査。protected environment の実 release run が残る。 |
| PKG-01 | 部分完了 | DMG/ZIP/AppImage の install/launch/content/uninstall smoke を [`package-smoke.sh`](../../../scripts/package-smoke.sh)、[Arch smoke](../../../.github/workflows/arch-package-smoke.yml)、[artifact workflow](../../../.github/workflows/build-artifacts.yml) に実装。`makepkg --cleanbuild`がRust `./src`を削除する`$srcdir`衝突を検出し、[`PKGBUILD`](../../../PKGBUILD)のfail-fast、[README](../../../README.md)とworkflowの隔離`BUILDDIR`で修正した。native aarch64 clean containerでは、公式Bun 1.3.9をSHA-256固定したpacman providerとして導入後、依存検査を省略しない`makepkg --syncdeps --noconfirm --cleanbuild`、payload、install、fresh DB、019→28 upgrade、restart、uninstall後DB保持が全て成功（27,164,200 / 7,211,400 bytes）。公式x86_64およびmacOS / Windows hosted runner完走が残る。 |
| DEP-01 | 完了（ローカル検証済み） | Rust/Bun/toolchain、locked git revision、feature inventory、deny/advisory/update policy を [`rust-toolchain.toml`](../../../rust-toolchain.toml)、[`Cargo.toml`](../../../Cargo.toml)、[`deny.toml`](../../../deny.toml)、[dependency policy](../../security/dependency-policy.md)、[audit workflow](../../../.github/workflows/dependency-audit.yml) に固定。 |
| DOC-01 | 完了（ローカル検証済み） | 現行構成、Virtuoso retention、YQ multi-accountと未実装`from`の分離、portable/release/incident/security運用を[architecture](../../architecture.md)、[ADR index](../../adr/README.md)、[release runbook](../../release-runbook.md)、[README](../../../README.md)へ更新。link checkに加えarchitecture-sensitive diffでADR更新を必須にするCI gateを追加。 |
| BUDGET-01 | 部分完了 | 20k/420k/1M DB、startup/read-model/stream/media/YQ/bundle/package budget、main比較に加え、隔離`HOME`のApplication Supportを使う`.app`で実WKWebViewのstream/scroll/profile frame・commit・RSSとstartup before/afterを採取する[`benchmark-webview.mjs`](../../../scripts/benchmark-webview.mjs)を実装。macOS local artifactはstream commit/frame p95 10/33ms、scroll frame 66ms、profile 8/4ms。startupはbundle/interactive改善とmodule/commit/RSS退行を両方記録した。PERF-04の未達metricと継続CI化が残る。 |

## 部分完了の主要残件

- backend architecture: provider購読計画を[`stream_subscription.rs`](../../../src/application/desktop/stream_subscription.rs)へ、provider eventのsource付きUI変換・sequence・UI先行配信を[`stream_bridge.rs`](../../../src/application/desktop/stream_bridge.rs)へ、stream notificationのSQLite保存/native通知抑止を[`stream_notification.rs`](../../../src/application/desktop/stream_notification.rs)へ、package security fixture注入を[`release_security_smoke.rs`](../../../src/application/desktop/release_security_smoke.rs)へ分離し、Unified timeline sourceとActive account actorの境界を独立させた。`desktop.rs`に残るplatform runtime責務の分割と、transaction・外部API順序のcharacterization test拡充が残る。
- performance: Bluesky無変更1時間のAPI call削減が残る。Jetstream/firehoseやpoll間隔変更でUnified Timelineの更新鮮度を落とす代替は採用しない。
- 品質・observability・docs・budget: OS固有failure-mode testのhosted runner完走、実装とarchitecture記述の継続同期、実app cold-start/heapの継続budgetが残る。
- release/package: 署名・公証・protected environmentと3 OS package/WebView smokeは、実artifactを扱う外部runnerの成功証跡が揃うまで部分完了とする。

## 最終完了ゲート

`完了（ローカル検証済み）` はmacOS上の統合gateを通した判定であり、3 OS package、署名、公証、protected release環境の成功を含まない。`部分完了` は上記残件、各行の実計測、Windows/macOS/Linux package runnerまたは署名環境の証跡が揃うまで完了扱いにしない。
